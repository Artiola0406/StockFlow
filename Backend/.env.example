# Kopjoje si .env dhe plotëso vlerat (mos e commit-o .env).

# Lokal (nga kompjuteri): përdor External Database URL nga Render → PostgreSQL → Connections.
# Shembull formati:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST.region-postgres.render.com/DATABASE

DATABASE_URL=
